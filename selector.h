// The effect selector is dependent on the effects we have defined, as it
// combines the base, the layer and the mixer in ways that makes sense.
#ifndef SELECTOR_H
#define SELECTOR_H
#include "includes.h"
#include <FastLED.h>
